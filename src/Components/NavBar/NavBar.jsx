const NavBar = (props) => {
  return (
    <div>
      <h3 style={{ margin: "1em", cursor: "pointer" }}>
        Social
        <small className="text-muted">Feed</small>
      </h3>
    </div>
  );
};

export default NavBar;
